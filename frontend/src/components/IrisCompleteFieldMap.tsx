import { useMemo, useState } from "react";
function formatExact(value: any) { if (typeof value === "string") return value; if (value === undefined) return "undefined"; return JSON.stringify(value); }
function FieldTree({ value, path, evidence }: { value: any; path: string; evidence?: string }) { const [open, setOpen] = useState(false); const currentEvidence = value && typeof value === "object" && typeof value.evidence_state === "string" ? value.evidence_state : evidence; if (value === null || value === undefined || typeof value !== "object") return <div className="iris-field-row"><code>{path}</code><span className="iris-field-value">{formatExact(value)}</span>{currentEvidence && <small>{currentEvidence}</small>}</div>; const entries = Array.isArray(value) ? value.map((item, i) => [String(i), item] as const) : Object.entries(value); return <details className="iris-field-node" open={open} onToggle={e => setOpen(e.currentTarget.open)}><summary><code>{path}</code><span>{Array.isArray(value) ? `[${value.length}]` : `{${entries.length}}`}</span>{currentEvidence && <small>{currentEvidence}</small>}</summary><div className="iris-field-children">{entries.map(([key, child]) => <FieldTree key={`${path}.${key}`} value={child} path={`${path}.${key}`} evidence={currentEvidence}/>)}</div></details>; }
function SourceFieldExplorer({ architecture }: { architecture: any }) { const [product, setProduct] = useState("all"); const [query, setQuery] = useState(""); const [showProtected, setShowProtected] = useState(false); const fields = (architecture?.provider_payload_fields ?? []).filter((field: any) => { if (product !== "all" && field.product !== product) return false; if (!showProtected && field.protected) return false; return !query || `${field.product} ${field.path} ${field.field_name}`.toLowerCase().includes(query.toLowerCase()); }); const products = architecture?.products_and_capabilities ?? []; return <div className="iris-plaid-field-explorer"><div className="iris-complete-field-controls"><strong>Complete Plaid source-field universe</strong><span>{architecture?.counts?.unique_provider_fields ?? 0} unique provider fields · {architecture?.counts?.records ?? 0} current records · max payload depth {architecture?.counts?.max_payload_depth ?? 0}</span></div><div className="iris-complete-field-controls"><select value={product} onChange={e => setProduct(e.target.value)}><option value="all">All observed products / capabilities</option>{products.map((p: string) => <option key={p} value={p}>{p}</option>)}</select><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search exact provider field name or path"/><button type="button" onClick={() => setShowProtected(v => !v)}>{showProtected ? "Hide protected fields" : "Show protected-field metadata"}</button></div><div className="iris-field-tier"><summary><strong>Provider fields — unchanged names</strong><span>{fields.length}</span></summary><div>{fields.map((field: any) => <details key={field.field_id} className="iris-field-node"><summary><code>{field.product}:{field.path}</code><span>{field.field_name}</span><small>{field.evidence_state}</small></summary><div className="iris-field-children"><div className="iris-field-row"><code>exact_provider_field_name</code><span className="iris-field-value">{field.exact_provider_field_name}</span></div><div className="iris-field-row"><code>source_table</code><span className="iris-field-value">{field.source_table}</span></div><div className="iris-field-row"><code>source_record_id</code><span className="iris-field-value">{field.source_record_id}</span></div><div className="iris-field-row"><code>item_id</code><span className="iris-field-value">{field.item_id}</span></div><div className="iris-field-row"><code>value</code><span className="iris-field-value">{field.protected ? "[protected field value not exposed]" : formatExact(field.value)}</span></div><div className="iris-field-row"><code>canonical_path</code><span className="iris-field-value">{field.canonical_path}</span></div><div className="iris-field-row"><code>lineage</code><span className="iris-field-value">{field.lineage}</span></div></div></details>)}</div></div></div>; }
function DerivedFieldExplorer({ architecture }: { architecture: any }) { const [domain, setDomain] = useState("all"); const fields = (architecture?.derived_fields ?? []).filter((field: any) => domain === "all" || field.domain === domain); const domains = architecture?.domains ?? []; return <div className="iris-plaid-field-explorer"><div className="iris-complete-field-controls"><strong>Iris additive intelligence fields</strong><span>{architecture?.derived_field_count ?? 0} derived fields · {architecture?.domains?.length ?? 0} domains · {architecture?.subdomains?.length ?? 0} domain/analysis paths · operations: {(architecture?.operations ?? []).join(", ") || "none"}</span></div><div className="iris-complete-field-controls"><select value={domain} onChange={e => setDomain(e.target.value)}><option value="all">All Iris intelligence domains</option>{domains.map((d: string) => <option key={d} value={d}>{d}</option>)}</select></div><div className="iris-field-tier"><summary><strong>Derived fields</strong><span>{fields.length}</span></summary><div>{fields.map((field: any) => <details key={field.field_id} className="iris-field-node"><summary><code>{field.path}</code><span>{field.domain}</span><small>{field.evidence_state}</small></summary><div className="iris-field-children"><div className="iris-field-row"><code>field_name</code><span className="iris-field-value">{field.field_name}</span></div><div className="iris-field-row"><code>subdomain / analysis</code><span className="iris-field-value">{field.subdomain}</span></div><div className="iris-field-row"><code>analysis_id</code><span className="iris-field-value">{field.analysis_id ?? "not mapped"}</span></div><div className="iris-field-row"><code>operation</code><span className="iris-field-value">{field.operation}</span></div><div className="iris-field-row"><code>value</code><span className="iris-field-value">{formatExact(field.value)}</span></div><div className="iris-field-row"><code>lineage</code><span className="iris-field-value">{formatExact(field.lineage)}</span></div></div></details>)}</div></div>; }
export function IrisCompleteFieldMap({ intel }: { intel: any }) { const plaidArchitecture = intel?.plaid_iris_field_map?.provider_field_universe; const irisArchitecture = intel?.iris_field_architecture; const sourceCount = plaidArchitecture?.counts?.unique_provider_fields ?? 0; const derivedCount = irisArchitecture?.derived_field_count ?? 0; const integrity = useMemo(() => ({ sourceFieldsPreserved: Boolean(plaidArchitecture?.exact_provider_field_names), sourceValuesExact: Boolean(plaidArchitecture?.exact_provider_values), noFabricatedValues: plaidArchitecture?.fabricated_values === false, additiveDerivedFields: irisArchitecture?.source_model === "Plaid fields are Iris input fields; Iris-derived fields are additive.", dynamicDepth: plaidArchitecture?.structural_depth?.dynamic === true && plaidArchitecture?.structural_depth?.derived_from_actual_observed_fields === true }), [plaidArchitecture, irisArchitecture]); return <section className="iris-complete-field-map"><div className="iris-complete-field-header"><div><span className="eyebrow">FIELD-FIRST INTELLIGENCE ARCHITECTURE</span><h2>Plaid fields → Iris fields → intelligence domains → products</h2><p>Plaid is the complete provider/source field architecture. Iris preserves every applicable Plaid source field and adds new derived fields produced by evidence-gated intelligence domains and analyses. Products consume this field universe; products are not architecture tiers.</p></div><div className="iris-complete-field-stats"><b>{sourceCount.toLocaleString()}</b><span>Plaid source fields</span><b>{derivedCount.toLocaleString()}</b><span>Iris-derived fields</span></div></div><div className="iris-field-tier"><summary><strong>Architecture integrity</strong><span>{Object.values(integrity).every(Boolean) ? "PASS" : "LIMITED"}</span></summary><div>{Object.entries(integrity).map(([key, value]) => <div className="iris-field-row" key={key}><code>{key}</code><span className="iris-field-value">{value ? "PASS" : "LIMITED"}</span></div>)}</div></div>{plaidArchitecture && <SourceFieldExplorer architecture={plaidArchitecture}/>} {irisArchitecture && <DerivedFieldExplorer architecture={irisArchitecture}/>} {!plaidArchitecture && <div className="iris-field-tier"><summary><strong>Source field architecture unavailable</strong></summary><p>Current intelligence did not return the complete provider field universe.</p></div>}</section>; }
