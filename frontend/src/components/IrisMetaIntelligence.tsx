import type { ReactNode } from "react";

type Props = { intel: any };
const label=(s:any)=>String(s??"—").replaceAll("_"," ").replaceAll("-"," ").replace(/\b\w/g,c=>c.toUpperCase());
const panel={background:"#111722",border:"1px solid #2a3445",borderRadius:16,padding:18,color:"#eef2f8"};
const card={background:"#171e2a",border:"1px solid #2a3445",borderRadius:13,padding:14,color:"#eef2f8"};
function Section({title,children}:{title:string;children:ReactNode}){return <section style={panel}><div style={{display:"flex",justifyContent:"space-between",gap:12,alignItems:"baseline",marginBottom:12}}><span style={{fontSize:11,letterSpacing:1.5,opacity:.7}}>{title}</span></div>{children}</section>}
export function IrisMetaIntelligence({intel}:Props){const m=intel?.meta_intelligence,q=m?.quality,b=m?.bottlenecks??[],steps=m?.highest_leverage_next_steps??[],signals=m?.integrity_signals??[],c=m?.composition_readiness??{};return <section className="iris-meta-intelligence" style={{display:"grid",gap:14,marginTop:16}}><Section title="META-INTELLIGENCE · SYSTEM AWARENESS"><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(150px,1fr))",gap:10}}><article style={card}><small>Evidence coverage</small><strong style={{display:"block",fontSize:25,marginTop:6}}>{q?.evidence_coverage==null?"—":`${(q.evidence_coverage*100).toFixed(1)}%`}</strong></article><article style={card}><small>Analytical coverage</small><strong style={{display:"block",fontSize:25,marginTop:6}}>{q?.analytical_coverage==null?"—":`${(q.analytical_coverage*100).toFixed(1)}%`}</strong></article><article style={card}><small>Higher-order gate</small><strong style={{display:"block",fontSize:25,marginTop:6}}>{q?.higher_order_readiness?"Ready":"Constrained"}</strong></article><article style={card}><small>Possible compositions</small><strong style={{display:"block",fontSize:25,marginTop:6}}>{Number(c.possible_combinations??0).toLocaleString()}</strong></article></div><p style={{marginBottom:0,opacity:.75}}>This layer does not invent financial facts. It measures what Iris can currently establish, what is blocked, and which evidence would unlock the most additional analysis.</p></Section><Section title="HIGHEST-LEVERAGE EVIDENCE BOTTLENECKS">{b.length?<div style={{display:"grid",gap:9}}>{b.slice(0,8).map((x:any)=><article style={card} key={x.input}><div style={{display:"flex",justifyContent:"space-between",gap:12}}><strong>{label(x.input)}</strong><b>{label(x.leverage)}</b></div><small>{x.statement}</small><div style={{marginTop:7,opacity:.65}}>{x.analyses_blocked} analysis{Number(x.analyses_blocked)===1?"":"es"} blocked</div></article>)}</div>:<p style={{margin:0}}>No current evidence bottleneck is identified.</p>}</Section><Section title="NEXT-BEST EVIDENCE MOVES">{steps.length?<div style={{display:"grid",gap:8}}>{steps.slice(0,5).map((x:string,i:number)=><div style={card} key={`${i}-${x}`}>{String(i+1).padStart(2,"0")} · {x}</div>)}</div>:<p style={{margin:0}}>No additional evidence move is currently prioritized.</p>}</Section><Section title="INTEGRITY SIGNALS">{signals.length?<div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(200px,1fr))",gap:9}}>{signals.map((x:any)=><article style={card} key={x.key}><strong>{label(x.status)}</strong><div>{label(x.key)}</div><small>{x.statement}</small></article>)}</div>:<p style={{margin:0}}>No integrity signals returned.</p>}</Section></section>}
